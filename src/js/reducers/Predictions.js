Predictions = function reducer(state={
    data: {},
    votes: [],
  }, action) {
    // TODO: Update with proper cases
    switch (action.type) {
      case "ONE_THING" {
        return { ...state, var: value }
      }
    }
}

export default Predictions;