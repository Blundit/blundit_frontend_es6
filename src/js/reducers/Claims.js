Claims = function reducer(state={
    data: {},
  }, action) {
    // TODO: Update with proper cases
    switch (action.type) {
      case "ONE_THING" {
        return { ...state, var: value }
      }
    }
}

export default Claims;